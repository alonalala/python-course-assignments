# Day 08: Web Application with FastAPI

## Overview
This assignment splits a sequence profiler application into isolated core business logic and a web layer using **FastAPI**.

## How to Setup and Run
1. Navigate into this folder and install dependencies:
   ```bash
   python3 -m pip install -r requirements.txt

## AI Interaction Prompts
"How do I build a FastAPI application that takes a DNA string and a window size parameters through a POST request using Pydantic?"

"Show me how to use FastAPI's TestClient inside a pytest file to check if custom validation error codes function correctly."